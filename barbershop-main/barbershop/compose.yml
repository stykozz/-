services:
  db:
    image: mongo:6.0
    container_name: barbershop_db
    ports:
      - "27017:27017"

  web:
    build: .
    container_name: barbershop_web
    ports:
      - "3000:3000"
    environment:
      - DB_HOST=db
    depends_on:
      - db
